#include <iostream>
#include <cmath>
using namespace std;

float FindAbs( float a){
	float s;
	abs(a);
	return abs(a);
}

int main(){
	float a;
	cout<<"nhap a: "; cin>>a;
	cout<<"gia tri tuyet doi cua a la: "<<FindAbs(a);
}